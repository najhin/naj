[[redirects]]
  from = "/old-path"
  to = "/new-path"
  status = 301
  force = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"

[build]
  publish = "dist"
  command = "npm run build"